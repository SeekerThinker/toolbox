(() => {
  renderers.fivewhys = root => {
    root.innerHTML=`<div class="editor-stack"><div class="field"><label>问题 / 现象</label><input id="whyProblem" class="text-input" placeholder="例如：报告总是在截止前才开始写"></div><div class="why-stack">${Array.from({length:5},(_,i)=>`<div class="why-row"><span>为什么 ${i+1}</span><input class="text-input why-input" placeholder="${i===0?"因为……":"继续追问上一层原因……"}"></div>`).join("")}</div><div class="field"><label>可控制的根因 / 杠杆点</label><input id="whyRoot" class="text-input"></div><div class="field"><label>下一步验证动作</label><input id="whyAction" class="text-input"></div><div class="toolbar"><button id="whyBuild" class="primary-btn" type="button">生成分析</button><button id="whyCopy" class="secondary-btn" type="button">复制</button></div><div id="whyOut" class="output-panel"></div></div>`;
    const out=$("#whyOut",root),build=()=>{const ws=[...root.querySelectorAll(".why-input")].map(x=>x.value.trim()).filter(Boolean);out.textContent=`# 问题\n${$("#whyProblem",root).value.trim()||"（未填写）"}\n\n${ws.map((x,i)=>`${i+1}. 为什么？ ${x}`).join("\n")||"还没有追问原因。"}\n\n## 可控制的根因 / 杠杆点\n${$("#whyRoot",root).value.trim()||"（待判断）"}\n\n## 下一步验证\n${$("#whyAction",root).value.trim()||"（待设计）"}`};$("#whyBuild",root).onclick=build;$("#whyCopy",root).onclick=()=>copyText(out.textContent);
  };

  renderers.premortem = root => {
    root.innerHTML=`<div class="editor-stack"><div class="field"><label>计划 / 项目</label><input id="pmPlan" class="text-input" placeholder="例如：三周后按时上线新版网站"></div><div class="field"><label>提前想：最可能在哪些地方出问题？（每行一个）</label><textarea id="pmRisks" class="input-area" style="min-height:145px" placeholder="需求不断变化\n关键页面开发太晚\n没有真实用户测试"></textarea></div><div class="field"><label>最早会出现哪些预警信号？</label><textarea id="pmSignals" class="input-area" style="min-height:105px"></textarea></div><div class="field"><label>现在就能做的预防动作</label><textarea id="pmActions" class="input-area" style="min-height:105px"></textarea></div><button id="pmBuild" class="primary-btn" type="button">生成风险卡</button><div id="pmOut" class="output-panel"></div></div>`;
    $("#pmBuild",root).onclick=()=>{$("#pmOut",root).textContent=`# 事前风险推演：${$("#pmPlan",root).value.trim()||"未命名计划"}\n\n## 可能出问题的地方\n${mindLines($("#pmRisks",root).value).map(x=>`- ${x}`).join("\n")||"- （待补充）"}\n\n## 预警信号\n${mindLines($("#pmSignals",root).value).map(x=>`- ${x}`).join("\n")||"- （待补充）"}\n\n## 预防动作\n${mindLines($("#pmActions",root).value).map(x=>`- [ ] ${x}`).join("\n")||"- [ ] （待补充）"}`};
  };

  renderers.review = root => {
    root.innerHTML=`<div class="editor-stack"><div class="two-col"><div class="field"><label>周期</label><select id="rvPeriod" class="select-input"><option>今日</option><option>本周</option></select></div><div class="field"><label>精力 / 状态（1–5）</label><div id="rvEnergy" class="reflection-scale">${[1,2,3,4,5].map(n=>`<button class="mini-btn ${n===3?"active":""}" type="button" data-n="${n}">${n}</button>`).join("")}</div></div></div><div class="field"><label>完成了什么？</label><textarea id="rvWins" class="input-area" style="min-height:95px"></textarea></div><div class="field"><label>哪里卡住 / 消耗最大？</label><textarea id="rvBlocks" class="input-area" style="min-height:95px"></textarea></div><div class="field"><label>学到了什么 / 哪个判断需要更新？</label><textarea id="rvLearn" class="input-area" style="min-height:95px"></textarea></div><div class="field"><label>下一周期最重要的一件事</label><input id="rvNext" class="text-input"></div><div class="toolbar"><button id="rvBuild" class="primary-btn" type="button">生成复盘</button><button id="rvCopy" class="secondary-btn" type="button">复制</button><button id="rvDownload" class="secondary-btn" type="button">下载 Markdown</button></div><div id="rvOut" class="output-panel"></div></div>`;
    let energy=3;$("#rvEnergy",root).onclick=e=>{const b=e.target.closest("[data-n]");if(!b)return;energy=+b.dataset.n;root.querySelectorAll("#rvEnergy button").forEach(x=>x.classList.toggle("active",x===b))};
    const build=()=>{const text=`# ${$("#rvPeriod",root).value}复盘\n\n状态：${energy}/5\n\n## 完成 / 进展\n${mindLines($("#rvWins",root).value).map(x=>`- ${x}`).join("\n")||"- （空）"}\n\n## 卡点 / 消耗\n${mindLines($("#rvBlocks",root).value).map(x=>`- ${x}`).join("\n")||"- （空）"}\n\n## 学习 / 判断更新\n${mindLines($("#rvLearn",root).value).map(x=>`- ${x}`).join("\n")||"- （空）"}\n\n## 下一周期最重要的一件事\n${$("#rvNext",root).value.trim()||"（未填写）"}`;$("#rvOut",root).textContent=text;return text};
    $("#rvBuild",root).onclick=build;$("#rvCopy",root).onclick=()=>copyText(build());$("#rvDownload",root).onclick=()=>deskkitDownload(new Blob([build()],{type:"text/markdown;charset=utf-8"}),`review-${new Date().toISOString().slice(0,10)}.md`);
  };

  renderers.feynman = root => {
    root.innerHTML=`<div class="editor-stack"><div class="field"><label>我要理解的概念</label><input id="fyTopic" class="text-input" placeholder="例如：机会成本 / CAPM / Transformer 注意力"></div><div class="field"><label>1. 像给 12 岁的人一样解释</label><textarea id="fyExplain" class="input-area" style="min-height:125px"></textarea></div><div class="field"><label>2. 哪些地方说不清 / 依赖术语？</label><textarea id="fyGap" class="input-area" style="min-height:100px"></textarea></div><div class="field"><label>3. 查漏补缺后的例子 / 类比</label><textarea id="fyExample" class="input-area" style="min-height:100px"></textarea></div><div class="field"><label>4. 压缩成一句话</label><input id="fyOne" class="text-input" placeholder="如果只能记住一句话……"></div><div class="toolbar"><button id="fyBuild" class="primary-btn" type="button">生成学习卡</button><button id="fyCopy" class="secondary-btn" type="button">复制</button></div><div id="fyOut" class="output-panel"></div></div>`;
    const build=()=>{const text=`# 费曼学习卡：${$("#fyTopic",root).value.trim()||"未命名概念"}\n\n## 我的解释\n${$("#fyExplain",root).value.trim()||"（未填写）"}\n\n## 知识缺口\n${$("#fyGap",root).value.trim()||"（待检查）"}\n\n## 例子 / 类比\n${$("#fyExample",root).value.trim()||"（未填写）"}\n\n## 一句话版本\n${$("#fyOne",root).value.trim()||"（未填写）"}`;$("#fyOut",root).textContent=text;return text};$("#fyBuild",root).onclick=build;$("#fyCopy",root).onclick=()=>copyText(build());
  };
})();
