# Toolbox

一个轻量、开源、Local-first 的个人思考与执行系统。

**在线使用：** https://seekerthinker.github.io/toolbox/

## 核心原则

Toolbox 的核心是一件具体要做的事，但**不规定用户必须经过固定流程**。

一个任务最小只需要：

```text
id
title
status
createdAt
```

例如只写“写报告”，就可以立刻开始番茄钟。完成标准、下一步、估时、计划时间、优先级、风险分析、Decision Journal、复盘等全部是可选能力。

产品遵循三个原则：

- **Optional · 可选**：任何环节都不是前置条件；
- **Contextual · 带上下文**：从任务进入任何方法时自动携带已有信息；
- **Composable · 可组合**：某个方法产生的新信息可以无感写回任务，供其他能力继续使用。

任务的复杂度由事情本身决定，而不是由软件决定。

## 一个任务可以怎样生长

简单任务可以一直保持：

```text
买牛奶
→ 完成
```

也可以直接：

```text
写报告
→ 番茄钟 25 分钟
→ 完成
```

复杂任务则可以按需逐渐增加：

```text
准备季度汇报
├─ 完成标准
├─ 下一步动作
├─ 预计用时 / 计划开始
├─ 优先级
├─ 障碍 / If–Then
├─ 任务拆解
├─ Decision Journal
├─ 原因探索
├─ Pre-mortem
├─ 番茄钟 / 深度工作
└─ 完成复盘
```

这些能力没有固定顺序，可以从任何节点开始，也可以随时切换到另一个节点。

## 任务详情

打开任务后，默认只强调最直接的操作：

- 开始番茄钟；
- 开始深度工作；
- 标记完成；
- 删除。

其他内容采用按需展开：

- **补充任务信息**：完成标准、下一步、估时、计划时间、优先级、障碍、If–Then 和备注；
- **深入思考**：任务拆解、事前风险、Decision Journal、原因探索、阶段复盘、解释与学习；
- **完成后复盘**：只有任务完成后才出现，而且仍然可跳过；
- **历史**：番茄钟、深度工作、分析与复盘都汇到同一条任务时间线。

元认知提示也是建议层，不会阻止用户继续执行。

## 捕获与优先级

首页输入框支持一行一件事，可以一次粘贴多行形成多个待办。

四象限是待办集合的一种可选视图，不是必填步骤。任务只有在用户主动设置优先级后才进入四象限；没有设置的任务保持在“未分类”，不会默认被解释成“不重要 + 不紧急”。

## 当前方法能力

方法库默认折叠，更推荐从具体任务中按需调用。

目前共 **8 个方法**：

| 分类 | 方法 |
| --- | --- |
| **思维** | 任务拆解、Decision Journal、原因探索、事前风险推演、阶段复盘、解释与学习 |
| **专注** | 番茄钟、深度工作计时 |

Brain Dump 已吸收到捕获层，艾森豪威尔矩阵已吸收到任务视图，不再作为独立工具。

## 能力之间如何打通

- 任务拆解可以把完成标准、下一步和障碍直接写回任务；
- 番茄钟 / 深度工作优先使用 `nextAction`，没有下一步时直接使用任务标题；
- Decision Journal、原因探索、风险推演和复盘都自动带入当前 `taskId`；
- 阶段复盘会读取预计时间、实际专注投入和已有任务信息；
- 所有历史都继续挂在原任务下，不需要复制或重新建立上下文。

## 数据与账号同步方向

当前仍是 Local-first：数据保存在浏览器的 `toolbox:data:v1` 中，并支持完整 JSON 导入 / 导出。

任务与记录已经使用稳定 ID、`updatedAt`、`deletedAt`、`taskId` 等字段，为未来账号同步预留基础。

真正的账号 / 云同步尚未接入。未来仍坚持：

- 不登录也能完整使用；
- 登录只解决多设备连续性、迁移和备份；
- 同步任务、方法记录、专注历史、复盘和偏好；
- 不把本地备份冒充云同步。

## 技术结构

```text
index.html         # 待办优先的页面结构
styles.css         # 全部样式
core.js            # 方法注册、统一存储、任务上下文、数据备份
tasks.js           # 捕获、待办、四象限、可选能力、任务历史与复盘
tools-focus.js     # taskId / nextAction 关联的番茄钟与深度工作
tools-thinking.js  # 拆解、Decision Journal、原因探索、风险、复盘、学习
tests/smoke.mjs    # 核心产品模型检查
```

技术栈：**HTML + CSS + Vanilla JavaScript**。无框架、无 npm 依赖、无构建流程。

## 本地运行

```bash
python3 -m http.server 8080
```

然后访问 `http://localhost:8080`。

## 静态检查

```bash
node --check core.js
node --check tools-focus.js
node --check tools-thinking.js
node --check tasks.js
node tests/smoke.mjs
```

GitHub Actions 会在 push 和 pull request 时执行这些检查。

## 下一阶段

1. **可选账号与跨设备同步**；
2. 计划开始时间与 Google / Outlook / Apple Calendar 等日历打通；
3. Decision Journal 加入“当时判断 → 后来结果”的校准；
4. 对估时偏差、延期、反复卡点做长期趋势反馈；
5. 继续把可成为任务字段 / 视图的能力从独立方法中吸收出来；
6. PWA / 离线安装。

## License

MIT
