# Toolbox — AI 协作约定

本文件供参与 **SeekerThinker/toolbox** 的 AI 编码助手使用。产品介绍见 [README.md](README.md)，当前产品边界与已定决策见 [docs/PROJECT_CONTEXT.md](docs/PROJECT_CONTEXT.md)。参考 [HARC](https://github.com/ChongLiuPhil/Human-AI-Research-Collaboration-Protocol) 的“仓库承载可交接上下文”思路；**本项目不采用研究论文的框架审批、繁复记忆层或强制工作流**。

## 接手时

1. 读取 README、本文件和 `docs/PROJECT_CONTEXT.md`；按本次任务再读取相关源码和测试，不必一次读完整仓库。
2. GitHub `main` 的**最新源码和测试**是实现现状的依据；项目上下文记录产品意图与已定边界，不能代替实际代码核验。聊天摘要、旧文件摘录和先前 CI 结果可能过期。
3. 写入前重新获取目标文件 SHA；出现冲突则重读、合并，不强推或覆盖并发修改。只改 `SeekerThinker/toolbox`，不要顺手改独立仓库。
4. 不得把 AI 建议描述为用户已经批准的决定；未知事项注明待验证。涉及改变产品定位、登录/数据上传方式、清除历史或破坏性迁移，须有明确的用户决定。

## 不可默默改变的产品边界

- **任务优先**：用户输入几个字即可创建任务，直接开始番茄钟/深度工作；拆解、估时、计划、复盘全部可选，任务是上下文而不是流程锁。
- **当前公开版仅游客**：本地完整可用，公开 `index.html` / PWA 不加载账号与云端同步模块。邮箱认证和同步源码是休眠准备，不得宣称已对公众开放。
- **数据与隐私**：不自动上传、不静默覆盖本机数据；导入备份前明确确认覆盖；保护任务及专注历史。分享捕获只带入草稿，不自动创建任务，也不把分享正文塞入 URL。
- **主业克制**：方法库精选且次要，不为增加数量而加工具；计划/目标与长期校准各司其职。不要把独立 `word-writing-templates` 项目并入本仓库。
- **实现简洁**：保持静态 HTML/CSS/Vanilla JS、GitHub Pages 子路径兼容；除非有充分理由，否则不引入框架、构建链或另一个后端。

## 修改与验证

- 先识别变更所属：产品行为、界面文案/布局、数据/隐私、发布/运维；只读取受影响的文件。改变已定产品边界时先更新 `docs/PROJECT_CONTEXT.md` 的决策，再同步实现、README 和相关测试；实现状态以实际代码及当次验证为准。
- 最小改动并保留已有用户数据格式；若必须迁移，说明旧数据如何保留、回滚或备份。不得用隐藏按钮来代替删除危险行为，也不要把未启用功能做成看似可用的入口。
- 本地可运行时检查 JS 语法并执行 `tests/` 中相关脚本；提交后核对**最新提交 SHA** 对应的 GitHub Actions。更改公开资源时另核对 Pages 部署及 `sw.js` 缓存版本；CI 通过不等于移动端真实交互已测试。
- 汇报时分清**已修改、已验证、尚未验证/阻塞**；不声称实际设备测试、账号端到端测试或线上访问成功，除非有相应证据。

## 交接

重要产品决策或发布边界变化时，更新 `docs/PROJECT_CONTEXT.md`，使下一位协作者无需原聊天即可恢复方向。日常小修无需追加冗长日志；Git 历史记录具体修改，项目上下文只保留当前约束、已定决策和少量未解决风险。
